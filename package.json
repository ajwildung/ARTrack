{
  "name": "voltage-league",
  "private": true,
  "version": "0.1.0",
  "description": "Voltage League / AR go-kart — M1 Sprint Heat soft-visual loop (Phase 0)",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently -n api,ops,hud -c cyan,magenta,green \"npm run dev -w @voltage/backend\" \"npm run dev -w @voltage/ops\" \"npm run dev -w @voltage/hud\"",
    "build": "npm run build -w @voltage/ops && npm run build -w @voltage/hud",
    "start": "npm run start -w @voltage/backend",
    "test": "npm run test -w @voltage/backend",
    "smoke": "npm run smoke -w @voltage/backend",
    "lint": "npm run test -w @voltage/backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "typescript": "^5.7.3"
  },
  "engines": {
    "node": ">=22"
  }
}
